# Original reference

`CoreDebug.py` is the original DevProfiler v2 application supplied for the rewrite. The C# implementation retains its core layout and Python cProfile workflow while replacing system-wide CPU/RAM figures with target process-tree metrics, streaming output live, adding a Windows Job Object, and introducing a .NET 10 EventPipe adapter.
