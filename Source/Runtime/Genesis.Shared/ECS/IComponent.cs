namespace Genesis.Shared.ECS
{
    // Marker interface — all ECS components must implement this.
    // Components must be structs; IComponent carries no methods.
    public interface IComponent { }
}
